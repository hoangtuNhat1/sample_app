<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), 
             html: { method: :delete, class: "d-inline" }) do |f| %>
  <%= f.submit t("views.users.unfollow"), class: "btn btn-outline-danger btn-sm" %>
<% end %>
